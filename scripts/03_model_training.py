# Import necessary libraries and load the cleaned dataset
# Split the data into train and test sets
# Train multiple regression models (Linear Regression, Random Forest)
# Evaluate models using metrics (R2, MSE)
# Save trained models and metrics to `outputs/models/`
